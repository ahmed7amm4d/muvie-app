import React from "react";

const Trending = (props) => {
    return (
        <React.Fragment>
            <h1>Landing Page</h1>
        </React.Fragment>
    );
};

export default Trending;